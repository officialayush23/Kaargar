# Local development overrides — this file is gitignored by Vite automatically.
# It overrides .env so local dev hits the local backend instead of Render.
# DO NOT commit this file.

VITE_API_URL=http://localhost:8000/v1
